# Basic Plugin Template

This is a starter template for Invoices Reader plugins.

## Features
- Toolbar button to show a toast message.
- Settings checkbox field.

## Installation
1.  Copy this folder to your `plugins/` directory.
2.  Rename the folder to `my-plugin` (must match `id` in `manifest.json`).
3.  Restart Invoices Reader.
